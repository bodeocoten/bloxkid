repeat
    task.wait()
until game:IsLoaded() and game:GetService("Players").LocalPlayer
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

if
    (
        LocalPlayer.PlayerGui:FindFirstChild("Main (minimal)")
        and LocalPlayer.PlayerGui:FindFirstChild("Main (minimal)"):FindFirstChild("ChooseTeam")
    ) or LocalPlayer.Team == nil
then
    repeat
        task.wait()
        pcall(function()
            if LocalPlayer.PlayerGui:WaitForChild("Main (minimal)").ChooseTeam.Visible == true then
                for i, v in
                    pairs(
                        getconnections(
                            LocalPlayer.PlayerGui:FindFirstChild("Main (minimal)").ChooseTeam.Container["Pirates"].Frame.TextButton.Activated
                        )
                    )
                do
                    for a, b in pairs(getconnections(game:GetService("UserInputService").TouchTapInWorld)) do
                        b:Fire()
                    end
                    v.Function()
                end
            end
        end)
    until Players.LocalPlayer.Team ~= nil and game:IsLoaded()
end
local TargetRace = "Ghoul"
if not (game.Players.LocalPlayer.Data.Race.Value == TargetRace) then
repeat task.wait()
    game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("BlackbeardReward","Reroll", "2")
    task.wait(5)
until game.Players.LocalPlayer.Data.Race.Value == TargetRace
end
getgenv().key = "PREMIUM-E27F1C3E"
getgenv().SettingPullLever = {
    Team = "Pirates",
    BypassTeleport = false,
    PullLever = true,
}
loadstring(game:HttpGet("https://raw.githubusercontent.com/hihiae/36var/refs/heads/main/aduvipvaycu.lua"))()
